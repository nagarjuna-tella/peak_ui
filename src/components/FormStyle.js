// src/components/FormStyle.js

const formStyle = {
    container: {
        display: 'flex', 
        justifyContent: 'center', 
        alignItems: 'center', 
        height: '84vh' // Adjust the height as needed
    },
    box: {
        width: 300, // Adjust the width as needed
        padding: '20px',
        boxShadow: '0px 0px 10px 0px rgba(0,0,0,0.2)' // Add some shadow for depth
    }
};

export default formStyle;
